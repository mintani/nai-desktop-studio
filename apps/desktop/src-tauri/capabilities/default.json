{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the WebView is allowed to ask the Rust side for. shell:default lets links with target=\"_blank\" open in the default browser: the shell plugin's init script intercepts those clicks and hands http(s) URLs to the OS. Without it the WebView drops the click.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
