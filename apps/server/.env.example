# 生成サーバの設定。すべて任意で、未設定でも起動できる。
CORS_ORIGIN=http://localhost:3001
PORT=3000

# NovelAI の永続トークン。通常はアプリの設定画面から保存するので、ここに書く必要はない。
# 設定画面より優先度は低く、フォールバックとしてだけ使われる。
# NOVELAI_API_KEY=

# 設定ファイルの保存先を変えたいときだけ指定する（既定は ~/.config/nai-desktop-studio）。
# NAI_CONFIG_DIR=
